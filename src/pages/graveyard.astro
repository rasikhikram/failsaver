---
import Layout from '../layouts/Layout.astro';

const logo = "/vine-logo.svg";
const ghost = "/ghost-icon.svg";
---

<Layout>
    <div class="bg-black pb-16">
        <section class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-8 md:gap-16 py-16">
            <!-- Left Content -->

            <div class="flex-1 px-3">
                <h2 class="text-white text-4xl md:text-4xl lg:text-6xl font-extrabold mb-4">Graveyard</h2>
                <p class="text-3xl text-gray-400">
                    +200 analyses on why startups and company's projects fail.
                </p>
            </div>

            <!-- Right Newsletter Box -->
            <div class="flex-1 border border-gray-300 p-8 rounded-sm space-y-6">
                <h3 class="text-3xl font-bold text-white">
                    The <a href="#" class="underline font-bold">All-In-One</a> Newsletter for Startup Founders
                </h3>
                <p class="mt-2 text-gray-400 text-xl">
                    90% of startups fail. Learn how to not to with our weekly guides and stories.
                    <strong class="text-white">Join 40,000+ founders.</strong>
                </p>
                <form class="mt-4 space-y-3">
                    <input
                            type="email"
                            placeholder="Enter your email"
                            class="w-full px-3 py-4 text-xl bg-[#1b1b1b] text-white border border-gray-400 rounded-md focus:outline-none focus:ring-2 focus:ring-black"
                    />
                    <button
                            type="submit"
                            class="w-full bg-white text-black font-semibold py-4 rounded-md hover:bg-gray-200 transition text-xl font-bold"
                    >
                        Subscribe For Free →
                    </button>
                </form>
            </div>
        </section>

        <section class="bg-[#0e0e0e] text-white rounded-lg px-8 lg:py-24 md:p-12 max-w-7xl mx-auto flex flex-col md:flex-row items-center gap-8">
            <!-- Left Side -->
            <div class="flex-1">
                <h2 class="text-xl md:text-4xl lg:text-6xl font-extrabold mb-4">Startup Cemetery</h2>
                <p class="text-gray-400 text-2xl my-8">
                    Learn why +120 startups have shut down and avoid being part of the 90% of businesses that fail.
                </p>
                <a href="#"
                   class="text-white text-3xl font-bold hover:underline">
                    Go to Startup Cemetery →
                </a>
            </div>

            <!-- Right Side -->
            <div class="flex-1 flex justify-center items-center relative">
                <!-- Ghost -->
                <img src={ghost} alt="Ghost" class="absolute -right-6 -top-4 w-8 h-8" />

                <!-- Tombstone Card -->
                <div class="bg-white text-black flex flex-col items-center justify-center rounded-t-[50%] rounded-b-lg p-6 w-40 h-48">
                    <img src={logo} alt="Vine" class="w-12 h-12 mb-3" />
                    <h3 class="font-bold">Vine</h3>
                </div>
            </div>
        </section>

    </div>
</Layout>